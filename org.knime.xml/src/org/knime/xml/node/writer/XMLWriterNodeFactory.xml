<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE knimeNode PUBLIC "-//UNIKN//DTD KNIME Node 2.0//EN" "http://www.knime.org/Node.dtd">
<knimeNode type="Sink" icon="./xmlwrite.png">
    <name>XML Writer</name>
    <shortDescription>Writes XML Documents in a directory.</shortDescription>
    <fullDescription>
        <intro>
            The node takes the XML Documents of the selected column and writes
            them, each in a separate file, into a local or remote directory (denoted by a URL). The files will
            be named after the table's row IDs appended with ".xml".<br />
            Note that if the destination location is a remote URL not all options are available because in general it's
            not possible to determine whether the remote location exists. In this case it will always be overwritten.
		</intro>
        <option name="XML column">The column containing the XML cells.</option>
        <option name="Selected directory">Select the output directory here. You can also specify remote URLs, in this
        case the final destination URLs are built by directly appending the file name to this base URL.</option>
        <option name="Overwrite existing files">Choose if existing files 
            should be overwritten here. If this option is unchecked, the node
            will fail if a file already exists.</option>
    </fullDescription>

    <ports>
        <inPort index="0" name="Data Table">
            Input table containing at least one XML column.
        </inPort>
    </ports>
</knimeNode>


