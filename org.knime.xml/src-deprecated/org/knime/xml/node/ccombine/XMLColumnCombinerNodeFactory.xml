<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE knimeNode PUBLIC "-//UNIKN//DTD KNIME Node 2.0//EN" "http://www.knime.org/Node.dtd">
<knimeNode type="Manipulator" icon="./xmlcolcombiner.png" deprecated="true">
    <name>XML Column Combiner</name>
    <shortDescription>Merges XML columns in a single column.</shortDescription>
    <fullDescription>
        <intro>
            Does a row wise merge of XML columns. The created XML
            cells consist of a single XML element with custom name and attributes.
            The children of this element are the merged XML columns.
		</intro>
        <option name="New column name">
            Name of the new column in the output table.
        </option>
        <option name="Exclude / Include">
            The XML columns in the include list will be merged by this node.
        </option>
        <option name="Remove source columns">
            Check when the source columns should not show up in the output 
            table. Source columns are the the columns in the include list and the columns
            used for the <b>data bound name</b> and the <b>data bound attributes</b>.
        </option>        
        <option name="Element name">
            Name of the created XML element. A <b>custom name</b> will be equal
            for every row whereas <b>data bound name</b> takes its value from
            an input column.
        </option>           
        <option name="Data bound attributes">
            Attributes added to the XML element with data bound values can be 
            defined here.
        </option>
        <option name="Custom attributes">
        	Attributes added to the XML element can be 
            defined here.
        </option>

    </fullDescription>

    <ports>
        <inPort index="0" name="Input Table">
            Input table.
        </inPort>
        <outPort index="0" name="Output Table">
            The input table with additional column containing the merged XML columns.
        </outPort>        
    </ports>
</knimeNode>


